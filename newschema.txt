Session (managed by servlet container)
	contest           String
	username          String
	is_director       Boolean
	is_judge          Boolean
	is_contestant     Boolean

Contest [ id ]
	title             String
	created           Date
	created_by        String
	last_problem_id   Integer

User [ Contest.id / username ]
	password          String
	is_director       Boolean
	is_judge          Boolean
	is_contestant     Boolean

Problem [ Contest.id / id ]
	name              String
	created           Date
	visible           Boolean
	allow_submissions Boolean
	judged_by         String
	difficulty        Integer
	start_time        Date
	allocated_minutes Integer
	runtime_limit     Integer
	spec              Key(File)
	solution          Key(File)
